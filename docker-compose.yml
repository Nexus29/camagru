services:
  camagru:
    image: php:8.2-fpm-alpine
    container_name: camagru_app
    volumes:
      - ./src:/var/www/html
    networks:
      - camagru_network

  webserver:
    image: nginx:alpine
    container_name: camagru_web
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - camagru # Updated dependency mapping
    networks:
      - camagru_network

networks:
  camagru_network:
    driver: bridge

volumes:
  pg_data:
